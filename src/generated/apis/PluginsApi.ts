// tslint:disable
/**
 * Open Hospital API Documentation
 * Open Hospital API Documentation
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { Observable } from 'rxjs';
import type { AjaxResponse } from 'rxjs/ajax';
import { BaseAPI, throwIfNullOrUndefined, encodeURI } from '../runtime';
import type { OperationOpts, HttpHeaders } from '../runtime';
import type {
    PluginDefinition,
} from '../models';

export interface ProxyRequest {
    id: string;
    path: string;
}

export interface Proxy1Request {
    id: string;
    path: string;
}

export interface Proxy2Request {
    id: string;
    path: string;
}

export interface Proxy3Request {
    id: string;
    path: string;
}

export interface Proxy4Request {
    id: string;
    path: string;
}

export interface Proxy5Request {
    id: string;
    path: string;
}

export interface Proxy6Request {
    id: string;
    path: string;
}

/**
 * no description
 */
export class PluginsApi extends BaseAPI {

    /**
     * Returns all plugins that passed the startup health check and are currently available through the gateway.
     * List all registered plugins
     */
    listPlugins(): Observable<Array<PluginDefinition>>
    listPlugins(opts?: OperationOpts): Observable<AjaxResponse<Array<PluginDefinition>>>
    listPlugins(opts?: OperationOpts): Observable<Array<PluginDefinition> | AjaxResponse<Array<PluginDefinition>>> {
        const headers: HttpHeaders = {
        };

        return this.request<Array<PluginDefinition>>({
            url: '/plugins',
            method: 'GET',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Resolves the plugin by ID, checks authorization, then forwards the request to the plugin\'s upstream URL. The sub-path after /plugins/{pluginId} is appended to the plugin base URL. All HTTP methods are supported.
     * Proxy a request to an external plugin
     */
    proxy({ id, path }: ProxyRequest): Observable<string>
    proxy({ id, path }: ProxyRequest, opts?: OperationOpts): Observable<AjaxResponse<string>>
    proxy({ id, path }: ProxyRequest, opts?: OperationOpts): Observable<string | AjaxResponse<string>> {
        throwIfNullOrUndefined(id, 'id', 'proxy');
        throwIfNullOrUndefined(path, 'path', 'proxy');

        const headers: HttpHeaders = {
        };

        return this.request<string>({
            url: '/plugins/{id}/{path}'.replace('{id}', encodeURI(id)).replace('{path}', encodeURI(path)),
            method: 'GET',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Resolves the plugin by ID, checks authorization, then forwards the request to the plugin\'s upstream URL. The sub-path after /plugins/{pluginId} is appended to the plugin base URL. All HTTP methods are supported.
     * Proxy a request to an external plugin
     */
    proxy1({ id, path }: Proxy1Request): Observable<string>
    proxy1({ id, path }: Proxy1Request, opts?: OperationOpts): Observable<AjaxResponse<string>>
    proxy1({ id, path }: Proxy1Request, opts?: OperationOpts): Observable<string | AjaxResponse<string>> {
        throwIfNullOrUndefined(id, 'id', 'proxy1');
        throwIfNullOrUndefined(path, 'path', 'proxy1');

        const headers: HttpHeaders = {
        };

        return this.request<string>({
            url: '/plugins/{id}/{path}'.replace('{id}', encodeURI(id)).replace('{path}', encodeURI(path)),
            method: 'POST',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Resolves the plugin by ID, checks authorization, then forwards the request to the plugin\'s upstream URL. The sub-path after /plugins/{pluginId} is appended to the plugin base URL. All HTTP methods are supported.
     * Proxy a request to an external plugin
     */
    proxy2({ id, path }: Proxy2Request): Observable<string>
    proxy2({ id, path }: Proxy2Request, opts?: OperationOpts): Observable<AjaxResponse<string>>
    proxy2({ id, path }: Proxy2Request, opts?: OperationOpts): Observable<string | AjaxResponse<string>> {
        throwIfNullOrUndefined(id, 'id', 'proxy2');
        throwIfNullOrUndefined(path, 'path', 'proxy2');

        const headers: HttpHeaders = {
        };

        return this.request<string>({
            url: '/plugins/{id}/{path}'.replace('{id}', encodeURI(id)).replace('{path}', encodeURI(path)),
            method: 'PUT',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Resolves the plugin by ID, checks authorization, then forwards the request to the plugin\'s upstream URL. The sub-path after /plugins/{pluginId} is appended to the plugin base URL. All HTTP methods are supported.
     * Proxy a request to an external plugin
     */
    proxy3({ id, path }: Proxy3Request): Observable<string>
    proxy3({ id, path }: Proxy3Request, opts?: OperationOpts): Observable<AjaxResponse<string>>
    proxy3({ id, path }: Proxy3Request, opts?: OperationOpts): Observable<string | AjaxResponse<string>> {
        throwIfNullOrUndefined(id, 'id', 'proxy3');
        throwIfNullOrUndefined(path, 'path', 'proxy3');

        const headers: HttpHeaders = {
        };

        return this.request<string>({
            url: '/plugins/{id}/{path}'.replace('{id}', encodeURI(id)).replace('{path}', encodeURI(path)),
            method: 'DELETE',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Resolves the plugin by ID, checks authorization, then forwards the request to the plugin\'s upstream URL. The sub-path after /plugins/{pluginId} is appended to the plugin base URL. All HTTP methods are supported.
     * Proxy a request to an external plugin
     */
    proxy4({ id, path }: Proxy4Request): Observable<string>
    proxy4({ id, path }: Proxy4Request, opts?: OperationOpts): Observable<AjaxResponse<string>>
    proxy4({ id, path }: Proxy4Request, opts?: OperationOpts): Observable<string | AjaxResponse<string>> {
        throwIfNullOrUndefined(id, 'id', 'proxy4');
        throwIfNullOrUndefined(path, 'path', 'proxy4');

        const headers: HttpHeaders = {
        };

        return this.request<string>({
            url: '/plugins/{id}/{path}'.replace('{id}', encodeURI(id)).replace('{path}', encodeURI(path)),
            method: 'PATCH',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Resolves the plugin by ID, checks authorization, then forwards the request to the plugin\'s upstream URL. The sub-path after /plugins/{pluginId} is appended to the plugin base URL. All HTTP methods are supported.
     * Proxy a request to an external plugin
     */
    proxy5({ id, path }: Proxy5Request): Observable<string>
    proxy5({ id, path }: Proxy5Request, opts?: OperationOpts): Observable<AjaxResponse<string>>
    proxy5({ id, path }: Proxy5Request, opts?: OperationOpts): Observable<string | AjaxResponse<string>> {
        throwIfNullOrUndefined(id, 'id', 'proxy5');
        throwIfNullOrUndefined(path, 'path', 'proxy5');

        const headers: HttpHeaders = {
        };

        return this.request<string>({
            url: '/plugins/{id}/{path}'.replace('{id}', encodeURI(id)).replace('{path}', encodeURI(path)),
            method: 'HEAD',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Resolves the plugin by ID, checks authorization, then forwards the request to the plugin\'s upstream URL. The sub-path after /plugins/{pluginId} is appended to the plugin base URL. All HTTP methods are supported.
     * Proxy a request to an external plugin
     */
    proxy6({ id, path }: Proxy6Request): Observable<string>
    proxy6({ id, path }: Proxy6Request, opts?: OperationOpts): Observable<AjaxResponse<string>>
    proxy6({ id, path }: Proxy6Request, opts?: OperationOpts): Observable<string | AjaxResponse<string>> {
        throwIfNullOrUndefined(id, 'id', 'proxy6');
        throwIfNullOrUndefined(path, 'path', 'proxy6');

        const headers: HttpHeaders = {
        };

        return this.request<string>({
            url: '/plugins/{id}/{path}'.replace('{id}', encodeURI(id)).replace('{path}', encodeURI(path)),
            method: 'OPTIONS',
            headers,
        }, opts?.responseOpts);
    };

}
